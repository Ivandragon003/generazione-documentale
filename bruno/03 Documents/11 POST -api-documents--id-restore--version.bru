meta {
  name: POST /api/documents/:id/restore/:version
  type: http
  seq: 11
}

post {
  url: {{base_url}}/api/documents/{{document_id}}/restore/1
  body: none
  auth: none
}

headers {
  x-user: {{user}}
}
