meta {
  name: POST /api/templates/:id/restore/:version
  type: http
  seq: 9
}

post {
  url: {{base_url}}/api/templates/{{template_id}}/restore/1
  body: none
  auth: none
}

headers {
  x-user: {{user}}
}
