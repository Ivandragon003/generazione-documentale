meta {
  name: POST /api/templates/upload
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/templates/upload
  body: multipartForm
  auth: none
}

headers {
  x-user: {{user}}
}

body:multipart-form {
  name: Template caricato da upload
  file: @file(C:\Users\ivand\OneDrive\Desktop\mac-documents\storage\samples\template-upload-valido.md)
}

script:post-response {
  const data = res.getBody();
  if (data && data.id) bru.setVar('template_id', data.id);
}
